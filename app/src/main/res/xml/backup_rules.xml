<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- SharedPreferences are backed up by default; log file excluded. -->
    <exclude domain="file" path="relay.log" />
</full-backup-content>
